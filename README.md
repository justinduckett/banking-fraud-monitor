# Wealthsimple Fraud Monitor Project

A data engineering portfolio project that simulates financial transactions to build a fraud detection pipeline using Python, dbt Cloud, and BigQuery.

## Setup

1. Create a virtual environment: `python3 -m venv venv`
2. Install dependencies: `pip install -r requirements.txt`
3. Run data generation: `python generate_data.py`
